import { {{ capitalized_name }}Form } from '@/components/{{ capitalized_name }}Form';

export default function New{{ capitalized_name }}Page() {
  // TODO: Implement entity creation logic
  return (
    <main className="container mx-auto px-4 py-8">
      <h1 className="text-3xl font-bold mb-4">Create New {{ capitalized_name }}</h1>
      <{{ capitalized_name }}Form />
    </main>
  );
}
