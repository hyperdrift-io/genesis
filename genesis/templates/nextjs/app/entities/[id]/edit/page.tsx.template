import { {{ capitalized_name }}Form } from '@/components/{{ capitalized_name }}Form';

export default function Edit{{ capitalized_name }}Page({ params }: { params: { id: string } }) {
  // TODO: Implement entity edit logic
  return (
    <main className="container mx-auto px-4 py-8">
      <h1 className="text-3xl font-bold mb-4">Edit {{ capitalized_name }}</h1>
      <{{ capitalized_name }}Form id={params.id} />
    </main>
  );
}
